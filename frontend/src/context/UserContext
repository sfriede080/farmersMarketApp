import {createContext} from "react";

const UserContext = createContext({
    user: {name:"Guest", email: "", picture: null},
    isAuthenticated: false,
    isLoading: false
});

export default UserContext;


